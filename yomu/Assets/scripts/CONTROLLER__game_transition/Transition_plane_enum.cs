

public enum Transition_plane {

    local, // nao pega UI => tem que garantir que a ui consiga fazer a transicao completamente. a troca de ui pode acompanhar a transicao tendo tampem os steaps hide => show
    global, // pega UI

}
